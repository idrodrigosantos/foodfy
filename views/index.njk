{% extends "layout.njk" %}

{% block content %}
    <header>
        <div class="header-text">
            <h1>As melhores receitas</h1>
            <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
        </div>

        <div class="header-image-chef">
            <img src="/img/chef.png" alt="Desenho de um chef">
        </div>
    </header>

    <section>
        <h2>Mais acessadas</h2>

        <article class="recipes">
            {% for item in items %}
                <div class="recipe">
                    <img src="/img/recipes-small/{{item.image}}" alt="Imagem do prato {{ item.title }}">
                    <p class="recipe-name">{{ item.title }}</p>
                    <p class="recipe-chef">por {{ item.author }}</p>
                </div>
            {% endfor %}
        </article>
    </section>
    
    <script src="/js/scripts.js"></script>
{% endblock %}